<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sicau.dao.RunAndAchievementMapper">


    <select id="getAchievementId" resultType="java.lang.String">
        select achievements_id from runAndAchievements where run_id = #{runId}
    </select>
    <select id="selectAchievementId" resultType="java.lang.String">
        select achievements_id from runAndAchievements where run_id = #{runId}
    </select>
</mapper>